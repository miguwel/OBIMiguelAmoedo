#include <stdio.h>
int main ()
{
	int N,M,i,o;
	
	scanf("%d%d",&N,&M);
	
	int faixas[N-1];
	
	int pontf[N];

	int pontComp[M];

	
	for(i=0; i<N-1; i++)
		scanf("%d", &faixas[i]);	

	for(i=0; i<N;i++)
		scanf("%d", &pontf[i]);

	for(i=0; i<M;i++)
		scanf("%d", &pontComp[i]);

	for(i=0; i<N; i++)
	{
		for(o=0; o<M; o++)
		{
			//
		}
	}

	for(i=0; i<M; i++)
		{
			//
		}

	return 0;
}